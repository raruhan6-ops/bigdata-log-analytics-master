-- =============================================================================
-- Hive 初始化脚本 — 创建数据库
-- 由 docker-compose 首次启动时自动执行
-- 完整表结构见: data-warehouse/init_all.sql
-- =============================================================================
CREATE DATABASE IF NOT EXISTS log_dw
COMMENT '日志分析数据仓库';

SHOW DATABASES;
